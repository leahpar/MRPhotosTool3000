{% extends 'base.html.twig' %}

{% block body %}

    {% set url = ea_url()
        .setController('App\\Controller\\PhotoCrudController')
        .setAction('index') %}
    <a href="{{ url }}">Retour à l'admin</a>
    <br>


    <a href="{{ path('photo_censure', {id: photo.id}) }}">
        <img src="{{ photo.thumbnail('instagram') }}&rand={{ random() }}" ismap>
    </a>

    <br>
    <a href="{{ url }}">Retour à l'admin</a>
    <br>
    <br>
    <a href="{{ path('photo_censure', {id: photo.id, delete: true}) }}">Supprimer la censure</a>


{% endblock %}